import { sum } from './trial.js';

test.skip('adds two numbers together', () => {
    expect(sum(1, 2)).toBe(3);
})

test.skip('adds two numbers together', () => {
    expect(sum(10, 44)).toBe(54);
})